[C#言語2026 第06回]

# 第1タームを振り返る

## 1. キーポイントのおさらい

### 1.1 変数

* 変数を使うには「宣言(せんげん)」して「初期化」する必要がある<br>
  変数の宣言は次のように書く<br>
  &emsp;<code><span class="hljs-keyword">変数の型</span> 変数の名前 = 初期値;</code>
* 変数にデータを書き込む(代入する)には、`=`(イコール)記号を使って次のように書く<br>
  &emsp;<code>変数 = 数値、文章、別の変数など;</code>

### 1.2 型

* 「型」や「クラス」はデータの種類のことで、整数用の`int`(イント)、実数用の`float`(フロート)、<br>
  文章用の`string`(ストリング)などがある

### 1.3 if文

* if文を使うと「ある条件が正しい時だけ処理をする」というプログラムが書ける
* if文は次のように書く<br>
  <code><span class="hljs-keyword">if</span> (条件式) {<br>
  &emsp;&emsp;処理<br>
  &emsp;}</code>
* else句を使うと「条件式が正しくなかった時」の処理を書ける
* else句は次のように書く<br>
  <code><span class="hljs-keyword">if</span> (条件式) {<br>
  &emsp;&emsp;処理A<br>
  &emsp;} <span class="hljs-keyword">else</span> {<br>
  &emsp;&emsp;処理B<br>
  &emsp;}</code>
* 条件式が真のときは`true`、偽のときは`false`になる
* if文の中にif文を書いて、多重if文を作ることができる
* 論理演算子(`&&`や`||`)を使うと、複雑な条件をあらわすことができる

### 1.4 実行順序とスコープ

* プログラムは上から下に向かって、順番に実行される
* if文やelse句を使うと、プログラムの流れを分岐し、その中からひとつの流れを選べるようになる
* if文では、より厳しい条件を先にチェックする
* `{`と`}`で囲われた範囲を「ブロック」という
* 変数が使える範囲を「スコープ」という
* スコープは、「変数の宣言」で始まり、「同じレベルのブロックの`}`」で終わる

### 1.5 配列

* 配列は「同じ型のデータを複数まとめて扱う」機能
* 配列の宣言には次の２種類の書きかたがある<br>
  <code><span class="hljs-keyword">型</span>[] 配列名 = { データ0, データ1, ... };</code><br>
  <code><span class="hljs-keyword">型</span>[] 配列名 = new 型[データ数];</code>
* 配列の`a`番目の変数を読み書きするには次のように書く<br>
  <code>配列名[a];</code><br>
  このとき、`[`と`]`の中に書く数値のことを「添字(そえじ)」という
* 添字は`0`から数える<br>
  例えば長さ`5`の配列の場合、有効な添字は`0`,`1`,`2`,`3`,`4`で、`5`は使えない
* 配列名に続けて`.Length`(ドット・レングス)を書くと、配列の要素数を調べられる

### 1.6 for文

* for文の「真の能力」を使うには、`()`の中に次のようなプログラムを書く<br>
  &emsp;<code><span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = 0; i < n; i++)</code><br>
  &emsp;<code>{</code><br>
  &emsp;<code>&emsp;繰り返したいプログラム</code><br>
  &emsp;<code>}</code><br>
  ()内の変数`i`は「何回目の繰り返しか」を記録するもので、「カウンタ変数」や「ループ変数」と呼ばれる<br>
  `n`は「繰り返し回数」をあらわし、整数または変数で指定できる

<div style="page-break-after: always"></div>

## 2. 中間試験問題

クラス:____________________ &emsp; 名前:____________________

### 問題１：変数の宣言

文字列型の変数`neko`を宣言し、文字列`ミケ`で初期化する方法として、正しい番号を選びなさい。

1. <code>neko = <span class="hljs-string">"ミケ"</span>;</code>
2. <code>neko <span class="hljs-keyword">string</span> = <span class="hljs-string">"ミケ"</span>;</code>
3. <code><span class="hljs-keyword">string</span> neko <span class="hljs-string">"ミケ"</span>;</code>
4. <code><span class="hljs-keyword">string</span> neko = <span class="hljs-string">"ミケ"</span>;</code>

答え: ________

### 問題２：整数と実数の計算

以下の変数宣言について、変数`a`, `b`, `c`, `d`に代入される数値を答えなさい。

&emsp;<span class="hljs-keyword">int</span> a = 6 - 2 * 4;<br>
&emsp;<span class="hljs-keyword">int</span> b = 13 / 4;<br>
&emsp;<span class="hljs-keyword">float</span> c = 5 / 2.0f;<br>
&emsp;<span class="hljs-keyword">int</span> d = 10 % 3;

a:________ &emsp; b:________ &emsp; c:________ &emsp; d:________

### 問題３：型の選択

以下のC#プログラムを実行したところ、コンソール画面に「1年は365.25日」と表示されました。

&emsp;【　ア　】 a = <span class="hljs-string">"1年は"</span>;<br>
&emsp;【　イ　】 b = 365.25f;<br>
&emsp;【　ウ　】 c = <span class="hljs-string">"日"</span>;<br>
&emsp;Console.WriteLine(a + b + c);

空欄ア、イ、ウに当てはまる型の名前を答えなさい。

ア:________ &emsp; イ:________ &emsp; ウ:________

<div style="page-break-after: always"></div>

### 問題４：基本的なif文

以下のプログラムについて、入力が`0`の場合だけ、コンソール画面に「ゼロ」と表示したい。

&emsp;<span class="hljs-keyword">string</span> a = Console.ReadLine();<br>
&emsp;<span class="hljs-keyword">if</span> (a 【　ア　】 "0")<br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("ゼロ");<br>
&emsp;}

空欄アに当てはまる比較演算子を答えなさい。

ア:________

### 問題５：条件式

以下のプログラムについて、入力が10以上の場合は「その数は10以上」、10未満の場合は「その数は10未満」と、コンソール画面に表示したい。

&emsp;<span class="hljs-keyword">string</span> a = Console.ReadLine();<br>
&emsp;<span class="hljs-keyword">int</span> b = <span class="hljs-keyword">int</span>.Parse(a);<br>
&emsp;<span class="hljs-keyword">if</span> (【　ア　】)<br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("その数は10以上");<br>
&emsp;}<br>
&emsp;<span class="hljs-keyword">if</span> (【　イ　】)<br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("その数は10未満");<br>
&emsp;}<br>

空欄ア、イに当てはまる条件式を答えなさい。

ア:________ &emsp; イ:________

<div style="page-break-after: always"></div>

### 問題６：プログラムの実行順序

以下のプログラムについて、入力値が0～29なら`C`、30～79なら`B`、80以上なら`A`を、コンソール画面に表示したい。

&emsp;<span class="hljs-keyword">int</span> a = <span class="hljs-keyword">int</span>.Parse(Console.ReadLine());<br>
&emsp;<span class="hljs-keyword">if</span> (a >= 【　ア　】)<br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("【　イ　】");<br>
&emsp;}<br>
&emsp;<span class="hljs-keyword">else if</span> (a >= 【　ウ　】)<br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("【　エ　】");<br>
&emsp;}<br>
&emsp;<span class="hljs-keyword">else</span><br>
&emsp;{<br>
&emsp;&emsp;Console.WriteLine("C");<br>
&emsp;}

空欄ア、イ、ウ、エに当てはまる値または文字を答えなさい。

ア:________ &emsp; イ:________ &emsp; ウ:________ &emsp; エ:________

### 問題７：配列の宣言

以下は、配列`a`と`b`を宣言するプログラムです。

&emsp;【　ア　】 a = { "じゃがいも", "にんじん", "たまねぎ" };<br>
&emsp;<span class="hljs-keyword">int</span>[] b = 【　イ　】 <span class="hljs-keyword">int</span>[100];<br>

空欄ア、イに当てはまる型または機能を答えなさい。

ア:________ &emsp; イ:________

### 問題８：配列の添字

以下のプログラムを実行したとき、コンソール画面に表示される数値を答えなさい。

&emsp;<span class="hljs-keyword">int</span>[] a = { 8, 3, 2, 6, 1 };<br>
&emsp;Console.WriteLine(a[3]);

答え:________

<div style="page-break-after: always"></div>

### 問題９：for文

(1) 以下のプログラムを実行し、数字`2`を入力したところ、コンソール画面に`32`が表示されました。

&emsp;<span class="hljs-keyword">int</span> a = <span class="hljs-keyword">int</span>.Parse(Console.ReadLine());<br>
&emsp;<span class="hljs-keyword">int</span> b = 1;<br>
&emsp;<span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> c = 0; c < 【　ア　】; c++)<br>
&emsp;{<br>
&emsp;&emsp;b *= a;<br>
&emsp;}<br>
&emsp;Console.WriteLine(b);

空欄アに当てはまる数値または文章を答えなさい。

ア:________

<br>

(2) 上記のプログラムを実行して数字`3`を入力したとき、コンソール画面に表示される数値を答えなさい。

答え:________

### 問題１０：配列とfor文

以下のプログラムを実行したとき、コンソール画面に表示される数値を答えなさい。

&emsp;<span class="hljs-keyword">int</span>[] a = { 3, 2, 5, 1, 4 };<br>
&emsp;<span class="hljs-keyword">int</span> b = 0;<br>
&emsp;<span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> c = 0; c < a.Length; c++)<br>
&emsp;{<br>
&emsp;&emsp;b += a[c];<br>
&emsp;}<br>
&emsp;Console.WriteLine(b);

答え:________

